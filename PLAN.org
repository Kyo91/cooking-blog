#+title: Cooking Blog Implementation Plan
#+startup: overview

* Purpose

Build a small, reliable Scala 3 application for saving recipes, recording each
time a recipe is cooked, attaching photos and notes, importing recipe content
from external pages, and finding saved recipes from partial recollections.

The first release should optimize for quick capture from a phone and simple
self-hosting. The design should leave clear seams for stronger authentication,
cloud object storage, and more sophisticated search if they are needed later.

* Product Scope

** MVP outcomes

- Create, view, edit, and delete a recipe.
- Record a new cooking instance (a meal) for an existing recipe.
- Add notes and photos to a meal while cooking.
- Attach one or more web or book references to a recipe.
- Import readable text from web references in the background.
- Search recipe titles, descriptions, meal notes, reference details, and
  imported recipe text.
- Browse recipe cards ordered by most recently made or updated.
- View a recipe's complete history, including each meal's timestamp, notes, and
  photos.
- Choose a primary photo for a recipe, with a sensible automatic fallback.

** Deferred until after the MVP

- Per-user recipe ownership, sharing, comments, and public visibility.
- Native mobile applications.
- Meal planning, shopping lists, nutrition calculation, and ingredient scaling.
- Automated extraction into a normalized ingredient/instruction model.
- Image recognition or semantic/vector search.
- Importing recipes from scanned book pages.

* Confirmed Product and Technical Decisions

- PostgreSQL is the source of truth. Local development uses the PostgreSQL
  service in =compose.yaml= with development-only credentials.
- PostgreSQL full-text search plus trigram matching is sufficient for the first
  release and can be replaced or extended later.
- The browser UI is server-rendered HTML enhanced with HTMX and small amounts of
  CSS/JavaScript. This keeps the project primarily Scala while still supporting
  live search and photo uploads. Scala.js or TypeScript can be evaluated later
  if the browser experience outgrows this approach.
- Photos are stored on the local filesystem in development and the first
  deployment. A =PhotoStore= interface will allow replacement with S3-compatible
  storage. Expected media growth is no more than approximately 5 GB in the first
  year.
- Each uploaded photo is limited to 10 MB (10,000,000 bytes). Accepted formats
  are JPEG, PNG, and WebP, verified from decoded file content.
- Recipes, meals, and photos use permanent deletion; there are no audit or
  soft-deletion requirements. Deleting a recipe cascades to its meals and photo
  records, and the corresponding photo files are permanently cleaned up.
- Recipe titles are unique case-insensitively.
- Authentication is required from the first release. Development uses the
  dummy =admin= / =test= credentials. The cloud release may continue to use one
  preconfigured administrator account, but its password must come from a
  deployment secret and the production interpreter must include login abuse
  controls rather than exposing the development-only authenticator.
- Login sessions have an absolute lifetime of 24 hours and are persisted in
  PostgreSQL so they survive application restarts.
- Only the login route is public. Every HTML page, JSON endpoint, media route,
  health route, and logout action requires an authenticated session.
- Recipes are a shared collection: every authenticated user can see and modify
  every recipe. There is no per-user ownership filter.
- A book reference is stored as structured citation text; only URL references
  are scraped.
- The first release is built as a Docker image and run on this laptop. A later
  release will deploy the same image to a cloud environment.
- Local-filesystem photo storage remains supported for the simplest laptop
  release. Cloud deployments use a private S3-compatible bucket through the
  existing =PhotoStore= boundary. A pinned MinIO-compatible service may be used
  for development and compatibility tests, but is not a required laptop
  dependency or a production storage recommendation.
- Durable scraping is optional at startup. Disabling it starts no scraper HTTP
  client, polling fibers, or worker pool. URL references remain usable, and
  durable pending jobs remain queued for a later startup with scraping enabled.
- Automated backups are out of scope for the laptop phase. The later cloud
  deployment will move photos to cloud object storage and PostgreSQL backups to
  a managed third-party provider.

* Proposed Technical Architecture

** Runtime and libraries

- Scala 3 on a current LTS JDK.
- sbt with dependency versions centralized in =project/Dependencies.scala=.
- Cats Effect 3 for resource management, concurrency, and application lifecycle.
- http4s for the HTTP server, routes, multipart uploads, and HTTP client.
- Circe for JSON request/response encoding.
- doobie with HikariCP for PostgreSQL access.
- Flyway for ordered database migrations.
- ScalaTags for server-rendered HTML.
- HTMX for debounced search and small page updates.
- http4s authentication middleware backed by an =Authenticator[F]= interface.
- Weaver or MUnit Cats Effect for tests.
- Log4Cats with an SLF4J backend for structured logging.
- PureConfig or Ciris for validated environment-based configuration.

Pin exact versions when the project is initialized, rather than recording
versions in this plan, so the supported current releases can be checked at that
time.

** Components

#+begin_src text
Browser
  |
  v
http4s routes
  |-- Public login route
  |-- Authentication middleware
      |-- HTML controllers/templates
      |-- JSON CRUD API
  |
  v
Services (validation and use cases)
  |-- RecipeService
  |-- MealService
  |-- PhotoService ------> PhotoStore
  |-- SearchService
  |-- ScrapeService -----> http4s client
  |
  v
DAO algebra + doobie implementation
  |
  v
PostgreSQL <---- durable scrape-job worker
#+end_src

Keep HTTP, business logic, persistence, and external I/O separate. Define DAO
and storage interfaces in terms of =F[_]=, provide production interpreters, and
use transactional programs when a use case changes multiple tables.

** Suggested source layout

#+begin_src text
src/main/scala/.../
  Main.scala
  config/
  domain/
  repository/
  service/
  auth/
  scraping/
  storage/
  http/
    api/
    pages/
    templates/
src/main/resources/
  db/migration/
  public/
src/test/scala/.../
#+end_src

Use a single sbt module initially. Split modules only when compilation boundaries
or independent deployment make the extra structure worthwhile.

** Authentication boundary

- Define an =Authenticator[F]= algebra that verifies credentials and returns a
  small =Principal= value. The initial interpreter accepts only =admin= / =test=;
  keep those development credentials in typed development configuration rather
  than scattering them through route code.
- =GET /login= renders the login form and =POST /login= authenticates it. These
  are the only unauthenticated routes.
- On success, issue a secure, HTTP-only, same-site session cookie and redirect to
  the originally requested page or the home page.
- Store only a cryptographic hash of the opaque session token in PostgreSQL.
  Sessions expire 24 hours after login regardless of activity and therefore
  remain valid across application restarts.
- Wrap the composed HTML, API, media, static-resource, health, and logout routes
  in one authentication middleware boundary so a route cannot accidentally be
  left unprotected.
- Redirect unauthenticated browser requests to =/login=. Return =401
  Unauthorized= for unauthenticated JSON requests.
- =POST /logout= invalidates the session. It is itself an authenticated route.
- Periodically delete expired and invalidated session records.
- Protect all state-changing form and API requests against CSRF. Rotate the
  session identifier on login and do not place the password in logs.
- Keep recipe data shared rather than adding an owner/user foreign key. A future
  real authenticator can replace the dummy interpreter without changing recipe
  authorization.

* Data Model

Use UUID primary keys, =timestamptz= timestamps stored in UTC, and database
constraints for invariants. The application should supply a =Clock[F]= so time
is deterministic in tests.

** =auth_sessions=

- =token_hash text primary key=
- =principal text not null=
- =csrf_secret_hash text not null=
- =created_at timestamptz not null=
- =expires_at timestamptz not null=
- =invalidated_at timestamptz null=
- Index on =expires_at= for lookup and cleanup.

Generate session and CSRF secrets with a cryptographically secure random source,
send only the opaque session token to the browser, and persist only its hash.
Session validation requires a matching, non-invalidated row whose =expires_at=
is in the future. The initial principal is =admin=; the table deliberately does
not create per-user recipe ownership.

** =recipes=

- =id uuid primary key=
- =title text not null=
- =description text not null default ''=
- =primary_photo_id uuid null=; add its foreign key after =photos= exists
- =created_at timestamptz not null=
- =updated_at timestamptz not null=
- =last_made_at timestamptz null=
- Unique index on =lower(title)=.

Do not make =last_made_at= the sole source of truth. It is a cached value for
sorting and must be updated in the same transaction as meal creation/deletion,
or recomputed from meals when needed.

** =meals=

- =id uuid primary key=
- =recipe_id uuid not null references recipes(id) on delete cascade=
- =notes text not null default ''=
- =cooked_at timestamptz not null=
- =created_at timestamptz not null=
- =updated_at timestamptz not null=
- Index on =(recipe_id, cooked_at desc)=.

Use =cooked_at= separately from =created_at= so an older cooking event can be
entered later.

** =photos=

- =id uuid primary key=
- =meal_id uuid not null references meals(id) on delete cascade=
- =storage_key text not null unique=
- =original_filename text not null=
- =content_type text not null=
- =byte_size bigint not null=
- =width integer null=
- =height integer null=
- =comment text null=
- =created_at timestamptz not null=
- =updated_at timestamptz not null=
- Index on =(meal_id, created_at)=.

Omit =recipe_id= because it can be reached through =meal_id=; storing both would
allow them to disagree. Check in the service that a selected primary photo
belongs to the recipe.

** =recipe_references=

- =id uuid primary key=
- =recipe_id uuid not null references recipes(id) on delete cascade=
- =kind text not null= constrained to =url= or =book=
- =url text null=
- =citation text null=
- =display_name text null=
- =created_at timestamptz not null=
- =updated_at timestamptz not null=
- Constraint requiring a URL for =url= and citation text for =book=.
- Unique normalized URL per recipe where kind is =url=.

References are a separate table because a recipe may have multiple sources.

** =scraped_documents=

- =id uuid primary key=
- =reference_id uuid not null unique references recipe_references(id) on delete
  cascade=
- =source_url text not null=
- =resolved_url text null=
- =title text null=
- =content_text text not null=
- =content_hash text not null=
- =http_etag text null=
- =http_last_modified text null=
- =scraped_at timestamptz not null=
- =updated_at timestamptz not null=

Keep raw HTML out of the initial database unless debugging shows it is needed.
Store extracted readable text and source metadata.

** =scrape_jobs=

- =id uuid primary key=
- =reference_id uuid not null references recipe_references(id) on delete cascade=
- =status text not null= constrained to =pending=, =running=, =succeeded=, or
  =failed=
- =attempt_count integer not null default 0=
- =available_at timestamptz not null=
- =claimed_at timestamptz null=
- =finished_at timestamptz null=
- =last_error text null=
- =created_at timestamptz not null=
- =updated_at timestamptz not null=
- Partial index over runnable jobs.

A durable database queue prevents imports from disappearing on restart. Claim
jobs with =FOR UPDATE SKIP LOCKED= so worker concurrency can be increased safely.

** =recipe_keywords=

- =id uuid primary key=
- =recipe_id uuid not null references recipes(id) on delete cascade=
- =keyword text not null= with a check requiring non-blank trimmed text.
- Unique index on =(recipe_id, lower(keyword))=.

Recipes may have user-supplied keyword tags so search does not depend only on
descriptions and successfully scraped pages. Recipe input accepts keywords as a
comma-separated string. Split on commas, trim surrounding whitespace from each
entry, discard empty entries, preserve multi-word keywords, and deduplicate
case-insensitively. For example, =sous vide, chicken,bbq,grill= becomes
=["sous vide", "chicken", "bbq", "grill"]=.

** Search document

Create a denormalized =recipe_search_documents= table (or materialized search
column) containing one row per recipe:

- =recipe_id uuid primary key=
- =plain_text text not null=
- =search_vector tsvector not null=
- =updated_at timestamptz not null=

Build =plain_text= from the title, description, recipe keywords, reference
labels/citations, scraped text, and meal notes. Maintain it explicitly after
relevant writes so the behavior is easy to test. Add a GIN index to
=search_vector= and a trigram index for title/phrase fallback. Rank title and
keyword matches above description, notes, and scraped content.

* API and Page Design

Use JSON under =/api/v1= and HTML pages outside that prefix. Return a consistent
JSON error body containing a machine-readable code, message, and optional field
errors. Validate IDs, text lengths, media types, upload sizes, and pagination
parameters at the boundary. Except for =GET /login= and =POST /login=, all
routes below require an authenticated session.

** Recipe endpoints

- =POST /api/v1/recipes=
- =GET /api/v1/recipes/:id=
- =PATCH /api/v1/recipes/:id=
- =DELETE /api/v1/recipes/:id=
- =GET /api/v1/recipes?q=&sort=&limit=&cursor==
- =POST /api/v1/recipes/:id/references=
- =PATCH /api/v1/recipes/:id/references/:referenceId=
- =DELETE /api/v1/recipes/:id/references/:referenceId=
- =POST /api/v1/recipes/:id/references/:referenceId/scrape= to retry or refresh
- =PUT /api/v1/recipes/:id/primary-photo/:photoId=

Creating or updating a URL reference should commit the reference and a pending
scrape job in one transaction. The response need not wait for scraping; return
the reference with its import status.

** Meal and photo endpoints

- =POST /api/v1/recipes/:recipeId/meals=
- =GET /api/v1/recipes/:recipeId/meals/:mealId=
- =PATCH /api/v1/recipes/:recipeId/meals/:mealId=
- =DELETE /api/v1/recipes/:recipeId/meals/:mealId=
- =POST /api/v1/recipes/:recipeId/meals/:mealId/photos= using multipart form data
- =PATCH /api/v1/recipes/:recipeId/meals/:mealId/photos/:photoId=
- =DELETE /api/v1/recipes/:recipeId/meals/:mealId/photos/:photoId=
- =GET /media/:photoId= or a storage-backed signed/static URL.

Although CRUD operations are scoped beneath a recipe in URLs, repositories can
remain table-oriented. Services enforce that nested IDs belong together.

** Browser pages

- Home page: search input, =+= action, sort control, and responsive recipe-card
  grid.
- Search partial: debounced requests while typing, with an empty-state action
  that carries the query into the new-recipe form.
- New/edit recipe page: title, description, and repeatable URL/book references.
- Recipe detail page: primary image, source/import status, imported content, and
  meal timeline.
- New/edit meal page: cooked date/time, notes, camera-friendly multi-photo
  upload, previews, and captions.

*** Frontend follow-up

The first browser implementation may use a small amount of progressively
enhancing JavaScript alongside server-rendered ScalaTags pages. Keep that code
as a cohesive static asset rather than growing multiple inline page scripts.
If browser-side form state, uploads, and live updates become materially more
complex, evaluate Scala.js as the next frontend boundary; do not introduce it
until that complexity justifies its build and maintenance cost.

Action item from Railway staging validation (2026-07-29): fix the recipe/meal
edit date-time parsing path. Entering =07/27/2026, 08:00 PM= in the browser
picker was rejected with =must be a date and time=. Preserve timezone-aware
=cooked_at= semantics and add browser plus API coverage for the picker format.

The =+= link should encode the current query as a parameter such as
=/recipes/new?title=grilled%20chicken= and the server must still validate it.

Deferred UI follow-up from staging review (2026-07-30): collapse the
rarely-used add/edit-source interface on the recipe detail page behind a compact
disclosure or explicit add-source action, while keeping source status visible.
On mobile, adjust recipe-description typography so it forms a more deliberate,
neatly justified reading column without harming readability or accessibility.

* Background Scraping

** Processing flow

1. Save the recipe/reference and enqueue a scrape job transactionally.
2. A supervised Cats Effect worker pool polls and atomically claims ready jobs.
3. Fetch the URL with strict time, redirect, response-size, and concurrency
   limits.
4. Prefer a discoverable print page only when it remains on an allowed HTTP(S)
   destination and yields useful content.
5. Extract metadata and readable text; first inspect schema.org
   =Recipe= JSON-LD, then fall back to main-content extraction.
6. Upsert the scraped document and rebuild the recipe search document in one
   transaction.
7. Mark the job successful. On failure, record a bounded error and retry with
   exponential backoff plus jitter up to a configured limit.
8. Show =pending=, =running=, =failed=, and =complete= status in the UI, with a
   manual retry action.

** Safety and etiquette

- Accept only =http= and =https= URLs.
- Resolve DNS and reject loopback, link-local, private-network, metadata-service,
  and other non-public destinations before every request and redirect to prevent
  SSRF.
- Set a descriptive user agent and conservative per-host concurrency.
- Enforce connection, request, and total-job timeouts.
- Cap redirects and downloaded bytes; reject unsupported content types.
- Sanitize all extracted content and render it as text, never trusted HTML.
- Avoid logging page contents, credentials, or URL query secrets.
- Decide and document the desired robots.txt policy before enabling production
  scraping.

“Concurrent on submission” therefore means that imports begin asynchronously
and multiple independent jobs may run in a bounded worker pool. It should not
mean launching an unsupervised fiber whose work can be lost.

* Photo Handling

- Stream uploads to temporary storage and enforce the 10 MB per-file limit while
  streaming.
- Accept JPEG, PNG, and WebP only. Decode and inspect each file; do not trust
  only the browser-provided MIME type or filename.
- Generate display and thumbnail variants, strip unsafe metadata, and correct
  orientation.
- Write the object first, then insert metadata; compensate by removing the object
  if the database transaction fails.
- For deletion, remove the database association transactionally and perform
  idempotent object cleanup. Periodically detect orphaned objects.
- Serve immutable variants with cache headers and non-guessable storage keys.
- Automatically use the latest meal photo when no explicit primary photo is
  selected.

* Error Handling and Observability

- Model expected service errors as typed values: not found, conflict, validation,
  invalid relationship, unsupported media, and unavailable dependency.
- Map errors to stable HTTP statuses and JSON/HTML responses in one place.
- Attach a request/correlation ID to logs and responses.
- Log request completion, scrape job lifecycle, retry count, and unexpected
  failures without logging user content.
- Expose =/health/live= and =/health/ready=; readiness verifies required
  dependencies such as the database and writable photo store.
- Add basic metrics for request latency/errors, queue depth, scrape duration and
  outcomes, and photo processing failures.

* Security and Data Integrity

- Require authentication globally except for the two login routes, and verify
  this default-deny boundary with route tests.
- Treat =admin= / =test= as development-only bootstrap credentials. Before
  untrusted deployment, replace the dummy interpreter with a real credential
  store and password hashing without changing the authorization rule.
- Protect mutation routes from CSRF.
- Set secure headers and a restrictive content security policy.
- Parameterize every SQL query through doobie.
- Limit JSON bodies, multipart bodies, individual files, number of photos per
  request, and text field lengths.
- Validate every nested resource relationship.
- Use database foreign keys, checks, uniqueness rules, and transactions as the
  final enforcement layer.
- Back up both PostgreSQL and the photo store together; periodically test a
  restore.

* Testing Strategy

** Unit tests

- Domain validation and timestamp behavior using a deterministic clock.
- Search-document construction and ranking weights.
- Service behavior with in-memory/fake DAO and storage interpreters.
- Scraper extraction against checked-in HTML fixtures, including JSON-LD, print
  links, malformed pages, redirects, and oversized responses.
- Retry/backoff policy and terminal failures.

** Integration tests

- DAO contract suites against a real disposable PostgreSQL instance.
- Migration from an empty database.
- Transactional creation of references/jobs and meal/=last_made_at= updates.
- Full-text and fuzzy-search results for representative recall queries.
- Authentication tests proving that login is public, =admin= / =test= succeeds,
  invalid credentials fail, and every other registered route is protected.
- Session persistence across application restarts, absolute 24-hour expiry,
  logout, redirect, JSON =401=, and CSRF behavior.
- HTTP route tests for success, validation, not-found, conflict, and pagination.
- Multipart upload for JPEG/PNG/WebP, invalid image, 10 MB boundary, oversized
  file, and cleanup paths.
- SSRF protection, redirect validation, timeouts, and scrape job claiming.

** End-to-end tests

- Create a recipe from the current search text.
- Attach a fixture URL and observe asynchronous import completion.
- Add two separate meals with notes/photos and see both in the timeline.
- Search using title, meal-note, and imported-content terms.
- Select and remove a primary photo.

* Delivery Phases

** DONE Phase 0: Resolve product and deployment decisions

- Confirm PostgreSQL search, server-rendered HTML, and local photo storage.
- Define the authentication and shared-data authorization rules.
- Define the 24-hour durable session policy.
- Define the 10 MB JPEG/PNG/WebP upload policy and permanent-deletion behavior.
- Select a Docker image running on this laptop as the first deployment target.
- Defer managed backups and cloud object storage to the later cloud deployment.

Exit criterion: deployment constraints and destructive behavior are explicit.

** DONE Phase 1: Bootstrap and infrastructure

- Create the Scala 3/sbt project and package layout.
- Add formatting, linting, compiler warnings, and test configuration.
- Add typed configuration, structured logging, and graceful shutdown.
- Use the PostgreSQL service already defined in =compose.yaml=. The development
  connection is =jdbc:postgresql://localhost:5432/cooking_blog= with username
  =cooking_blog= and password =cooking_blog_dev=.
- Implement the authentication/session boundary and dummy =admin= / =test=
  authenticator with PostgreSQL-backed 24-hour sessions before adding
  application routes.
- Establish CI for formatting, compilation, unit tests, and integration tests.

Exit criterion: a clean checkout can start PostgreSQL, apply migrations, run the
service, authenticate through the only public route, and pass CI.

Implemented with Scala 3.8.4, sbt 1.12.0, Java 21-targeted bytecode, http4s,
Cats Effect, doobie/HikariCP, Flyway, Ciris, Log4Cats, ScalaTags, MUnit Cats
Effect, strict compiler warnings, Scalafmt, and GitHub Actions CI. The
=auth_sessions= migration, dummy authenticator, 24-hour database-backed
sessions, CSRF-protected logout, protected health routes, configuration,
structured logging, and graceful resource shutdown are in place. Classical
brace syntax is enforced with the Scala compiler's =-no-indent= option.

** DONE Phase 2: Persistence and domain core

- Add Flyway migrations for recipes, meals, photos, references, scraped
  documents, jobs, and search documents; the authentication-session migration
  already exists.
- Define opaque/validated IDs and domain records.
- Implement a DAO algebra and doobie interpreter for every table.
- Add transaction boundaries at the service/use-case layer.
- Add DAO contract and migration tests.

Exit criterion: all records can be created, read, updated, and deleted with
constraints and transactional invariants verified.

Implemented with an incremental migration for all seven application tables and
their foreign keys, checks, uniqueness rules, full-text/trigram indexes, and
cascades. Opaque UUID-backed identifiers, database-backed enums, domain
records, table-oriented DAO algebras, and doobie =ConnectionIO= interpreters
are in place. The persistence service owns transaction boundaries for recipe
and search-document creation, meal/=last_made_at= maintenance,
reference/scrape-job creation, and scraped-document/search updates.
PostgreSQL integration tests cover migrations, CRUD for every DAO, deletion,
case-insensitive title uniqueness, reference and photo constraints, foreign-key
rollback, and meal timestamp invariants.

** DONE Phase 3: Recipe, meal, and reference APIs

- Implement validation, typed errors, cursor pagination, and JSON error mapping.
- Implement recipe and reference CRUD.
- Implement meal CRUD and correct =last_made_at= maintenance.
- Publish an OpenAPI description or checked-in endpoint contract.

Exit criterion: API tests cover the standard CRUD lifecycle and error cases.

Implemented with authenticated JSON routes under =/api/v1=, CSRF protection
for every mutation, boundary validation, typed service errors, stable JSON error
responses, and opaque cursor pagination with query/sort binding. Recipe,
reference, and meal writes use service-owned transactions; URL references
enqueue pending scrape jobs atomically, relevant writes rebuild search
documents, and meal creation, editing, and deletion recompute =last_made_at=.
The checked-in OpenAPI 3.1 contract documents the endpoints and schemas.
PostgreSQL-backed API tests cover authentication, CSRF, validation, conflicts,
pagination, nested-resource relationships, reference/job behavior, CRUD
lifecycles, and =last_made_at= maintenance.

** DONE Phase 4: Photos

- Implement =PhotoStore= and local-filesystem interpreter.
- Add 10 MB streaming multipart validation and safe JPEG/PNG/WebP processing.
- Implement photo metadata CRUD, primary-photo selection, and cleanup behavior.
- Document the laptop media directory and the accepted lack of backups for this
  phase.

Exit criterion: photos can be uploaded from a phone-sized client, displayed,
captioned, selected as primary, and safely deleted.

Implemented with an =F[_]=-style photo-storage boundary and a local-filesystem
interpreter using opaque storage keys and atomic variant writes. Multipart
uploads stream through temporary storage with a strict 10,000,000-byte
per-photo limit; JPEG, PNG, and WebP are accepted only after decoding. Images
are orientation-corrected, metadata-stripped, and re-encoded into original,
display, and thumbnail variants. WebP input is normalized to PNG so processing
remains pure Java and portable across the laptop's ARM architecture.

Authenticated APIs support multi-photo upload, caption editing, permanent
deletion, and recipe primary-photo selection. Media routes serve immutable
variants and automatically fall back to the newest photo from the most recent
meal. Recipe and meal deletion clean up corresponding objects, failed metadata
writes are compensated, and a periodic aged-orphan reconciliation handles
interrupted cleanup without racing active uploads. Readiness checks the photo
directory, configuration and no-backup behavior are documented, and
PostgreSQL-backed HTTP tests cover upload, display, caption, primary/fallback
selection, invalid and oversized content, and physical cleanup.

** DONE Phase 5: Durable scraping

- Implement transactional job enqueueing and concurrent job claiming.
- Implement secure HTTP fetching, print-page discovery, and content extraction.
- Add retry/backoff, stale-job recovery, refresh, and UI/API status.
- Rebuild search documents after imports.

Exit criterion: a submitted recipe returns immediately, survives an application
restart while pending, and later exposes searchable imported text.

Implemented with a supervised, bounded Cats Effect worker pool and PostgreSQL
=FOR UPDATE SKIP LOCKED= claims. URL-reference creation and refresh enqueue
durable jobs; concurrent claims exclude an already-running reference, abandoned
jobs are recovered after a configurable timeout, and retryable failures use
bounded exponential backoff with jitter before a terminal attempt limit.

HTTP fetching validates resolved addresses before every request and redirect,
rejects non-public networks and credential-bearing URLs, and bounds per-host
connections, redirects, response bytes, request time, and total attempt time.
Extraction prefers schema.org =Recipe= JSON-LD, falls back to readable main
content, and uses discoverable print pages only when they pass the same safety
checks and contain useful content. Successful document upsert, recipe search
document rebuild, and job completion commit in one transaction. The authenticated
scrape-status endpoint exposes pending, running, failed, and complete states plus
imported text, while the existing mutation endpoint supports manual refresh.

Unit and PostgreSQL-backed tests cover the Serious Eats-style recipe fixture,
fallback and print extraction, SSRF ranges and redirect validation, byte limits,
backoff, distinct atomic claims, stale recovery, retry, restart durability,
search-document rebuilding, and API status. The opt-in live test uses the
original Serious Eats URL and preserves a remote =403= as a terminal policy
response instead of bypassing it. The laptop robots policy and scraper limits
are documented in the README.

** DONE Phase 6: Search

- Implement weighted PostgreSQL full-text search and trigram fallback.
- Add recipe keyword persistence and comma-separated input normalization.
- Include recipe fields, keywords, reference information, scraped content, and
  meal notes.
- Add deterministic ranking and cursor pagination.
- Tune against a small representative set such as “grilled chicken,” including
  expected related recipes.

Exit criterion: acceptance queries find expected recipes with useful ordering
and acceptable interactive latency.

Implemented with PostgreSQL =websearch_to_tsquery= and weighted =tsvector=
ranking: titles have the highest weight, followed by recipe keywords, then
descriptions/references, and finally meal notes/imported text. A title trigram
fallback handles partial and misspelled recall. Recipe keywords are persisted
with database uniqueness constraints; API input accepts comma-separated text,
trims entries, drops blanks, and deduplicates case-insensitively. Search order
is deterministic, and cursors are bound to the normalized query and sort
order. PostgreSQL-backed API coverage verifies title priority, normalized
keyword matching, typo fallback, cursor behavior, and the representative
grilled-chicken query shape.

** DONE Phase 7: Browser experience

- Implement the responsive home grid and debounced search partial.
- Implement =+= prepopulation from the active query.
- Implement recipe detail/history and edit flows.
- Add keyword input to the new/edit recipe forms.
- Implement meal capture with photo previews and clear upload/import progress.
- Add accessible labels, keyboard navigation, focus handling, and empty/error
  states.

Exit criterion: the complete primary workflow is comfortable on both phone and
desktop without direct API use.

Implemented as server-rendered HTML enhanced by locally vendored HTMX and a
single authenticated static application asset. Page-oriented, CSRF-protected
form routes cover recipe, source, meal, caption, primary-photo, import retry,
and deletion flows while the JSON API remains unchanged. Browser JavaScript is
limited to HTMX glue, source rows, validation/focus handling, confirmation
dialogs, photo preview, and multipart upload progress.

Accessible error associations, invalid-field focus, live save/import/upload
status, visible focus styling, and responsive controls support keyboard and
phone use. PostgreSQL-backed Scala verification passes, as do Playwright flows
for search/create prepopulation, sources, responsive phone layout, import
state, meal/photo upload, captions, primary selection, and deletion. Manual
screen-reader, keyboard, and responsive smoke testing was accepted as adequate
for this stage.

** DONE Phase 8: Laptop deployment and release

- Build the application as a versioned Docker image.
- Add the application container to Compose with persistent local photo storage,
  PostgreSQL dependency/health checks, and non-development runtime secrets.
- Add health checks, metrics, operational logging, and alertable job failures.
- Add security tests, production limits, CSP/CSRF protections, and secrets
  handling.
- Run end-to-end tests against a production-like environment.
- Document that the laptop release has no backup guarantee and keep the service
  bound to a trusted interface while it uses the dummy authenticator.

Exit criterion: the versioned image can be deployed, observed, restarted without
losing sessions or persistent data, and upgraded with documented commands. The
accepted absence of laptop backups is clearly documented.

Implemented as a pinned =cooking-blog:0.1.0= image generated by
=sbt-native-packager= on a Java 21 runtime and executed as a non-root user. The
release Compose overlay binds the application only to =127.0.0.1=, keeps
PostgreSQL private, stores database and photo data in dedicated named volumes,
mounts passwords from read-only secret files, checks dependency and application
health, and hardens the container with a read-only root filesystem, bounded
temporary storage, dropped capabilities, and =no-new-privileges=.

Production configuration rejects development credentials, short login secrets,
relative photo storage, unbounded request sizes, and unsafe pool/scraper limits.
All responses carry a restrictive CSP and browser security headers, mutations
retain the global CSRF boundary, and request completion logs contain correlation
IDs without query text or user content. The authenticated Prometheus endpoint
reports request counts/latency, scrape outcomes/duration and queue state, and
photo-processing failures; terminal scrape failures are both logged and exposed
as an alertable gauge.

The clean Scala/PostgreSQL suite, security/configuration tests, generated image,
release Compose model, and five primary Playwright workflows pass. A
production-like isolated stack was authenticated with file-mounted random
secrets, created and searched data, exposed authenticated health/metrics, and
survived both an application restart and force-recreated upgrade without losing
the session or recipe. The release runbook documents build, startup,
observation, restart, upgrade, rollback, shutdown, secret rotation, the trusted
interface restriction, and the explicitly accepted absence of laptop backups.

The later cloud release must replace the development-named dummy authenticator
with a production-configured single-administrator interpreter before exposure
to an untrusted network, move photos to cloud object storage, configure managed
PostgreSQL backups, and perform a restore rehearsal.

** DONE Phase 9: Cloud-release decisions and configuration

- Preserve the existing =PhotoStore= algebra and add a storage-backend selector
  rather than introducing a second application-facing interface. Support
  =local= and =s3= backends with typed, validated configuration.
- Define S3 configuration for bucket, optional key prefix, region, optional
  endpoint override, path-style addressing, credentials, connection/request
  timeouts, and bounded concurrency. Require TLS for non-local endpoints and
  keep the bucket private.
- Perform a short implementation spike with =fs2-aws-s3=. It is the preferred
  streaming adapter, but do not force it if the selected stable release cannot
  supply the object metadata, paginated listing, readiness probe, or presigning
  behavior required by =PhotoStore=. In that case, use the AWS SDK v2
  =S3AsyncClient= directly behind the same algebra.
- Keep application-proxied, authenticated =/media= responses for the first
  cloud cutover. Evaluate short-lived presigned redirects separately if
  measured service egress or latency justifies them; never make the bucket
  public.
- Add =SCRAPE_ENABLED= with an explicit default. When false, do not allocate the
  scraper client or start worker fibers. Continue to enqueue jobs
  transactionally so no requested import is lost; expose them as queued while
  processing is disabled and drain them normally after re-enabling.
- Replace =DummyCredentialsAuthenticator= in production wiring with a clearly
  named configured-single-user interpreter. Continue constant-time credential
  comparison, database-backed 24-hour sessions, CSRF protection, and shared
  recipe access. Require a strong secret, secure cookies, and rate limiting or
  bounded backoff on failed login attempts.
- Define cloud production validation separately from laptop production:
  =AUTH_COOKIE_SECURE=true=, an HTTPS public origin, bounded request and
  connection pools, a non-development database secret, and a supported photo
  backend must all be mandatory.

Exit criterion: configuration tests prove that local, S3-compatible, scraper-on,
scraper-off, laptop, and cloud profiles either assemble the intended resources
or fail at startup with actionable errors.

Implemented with typed deployment-target, local/S3 photo-store, S3 credential,
endpoint, addressing, timeout, concurrency, and public-origin configuration.
Laptop production preserves absolute local storage and HTTP-only loopback
behavior; cloud production requires HTTPS origin semantics, secure cookies, a
private S3-compatible configuration, and non-development secrets. Selecting S3
reaches an explicit Phase 11 startup boundary rather than silently using local
storage.

=SCRAPE_ENABLED=false= allocates no scraper client, polling fiber, or worker
pool. URL references still enqueue durable jobs, the status API exposes whether
processing is enabled, the browser labels pending work as queued without active
polling, and a later enabled startup drains the backlog normally.

Production wiring now uses a configured single-administrator authenticator with
constant-time comparison and bounded process-local failure backoff while
development retains the =admin= / =test= dummy. The S3 client spike records the
stable =fs2-aws= decision gate without adding an unused dependency: Phase 11
must prove the complete =PhotoStore= contract before choosing the high-level
wrapper or the AWS SDK v2 asynchronous client directly.

Configuration, authentication, disabled-scraping API/browser behavior, clean
strict compilation, all 45 Scala/PostgreSQL tests, and an actual
=SCRAPE_ENABLED=false sbt run= startup were verified. PostgreSQL-backed suites
now run sequentially because they deliberately share one development schema.

** TODO Phase 10: Mobile-first browser experience overhaul

- Replace wordy, wrapping action controls with a coherent icon-first action
  system. Every control must retain a programmatic accessible name, visible
  keyboard focus, and a 44 by 44 pixel minimum touch target.
- Make the home page's recipe creation action a consistent add-recipe icon and
  remove interpolated empty-search copy such as =Create an empty title=. Keep the active
  query as prepopulated form data without making it the button label.
- Rework recipe, meal, and photo actions for small screens: primary-photo,
  caption/edit, and delete actions should be compact, discoverable, and clearly
  associated with the item they affect. Keep confirmation before permanent
  deletion.
- Add an authenticated photo download action that returns the original image
  with a safe download filename. Preserve authorization and avoid exposing a
  guessable storage key.
- Establish reusable visual tokens and browser-control patterns before adding
  further frontend features. Verify the primary workflows on real mobile
  viewports as well as desktop, including focus, touch targets, empty states,
  loading, and error states.
- Deliver the overhaul as three focused changes rather than one visual
  mega-change:
  1. Design foundation and home page: move the current inline browser styling
     into a dedicated asset, establish tokens/control classes, and redesign
     search, recipe cards, and the add-recipe action.
  2. Recipe and meal detail: introduce reusable ScalaTags helpers for
     icon-first controls/action trays, then update recipe, meal, source, and
     photo layouts.
  3. Photo download and interaction polish: add the authenticated original
     download route and refine overflow menus, confirmations, focus behavior,
     and mobile-browser coverage.
- Retain server-rendered ScalaTags and focused HTMX enhancement. Do not migrate
  to a client-side framework or Scala.js for this work; use native controls and
  the existing small browser asset for the limited interaction that remains.

Current progress (2026-07-30): Parts 1 and 2 are implemented. The former inline browser stylesheet is now the authenticated
=app-v1.css= asset, with reusable parchment/ink/herb/terracotta visual tokens,
responsive control and card patterns, visible focus states, reduced-motion
handling, and 44-pixel icon-control sizing. The home page has an icon-first,
programmatically named add-recipe action; search queries prepopulate its form
target both on initial server rendering and during live search without becoming
the button label. Recipe cards use a mobile-friendly two-column grid and are
each a single semantic link, so the photo and all card content open the recipe
without nested interactive controls. The empty state retains query
prepopulation while using the stable label =Add recipe=.

Recipe, source, meal, and photo actions now use reusable ScalaTags icon-action
and action-tray helpers. They retain explicit accessible names and confirmations
for permanent deletion, while photo captions and source edits use compact
associated editors. Long recipe titles wrap rather than crowd controls on a
phone.

ScalaFmt and all 52 Scala/PostgreSQL tests pass. Seven Playwright workflows
pass, including 390-pixel mobile checks for action targets, no horizontal
overflow, active-query prepopulation, whole-card recipe navigation, and
item-associated recipe-history actions.

Exit criterion: the home, recipe-detail, meal, and photo workflows are
comfortable and legible on a phone without text wrapping or crowded action
rows, and users can download an authenticated original photo in one action.

** TODO Phase 11: S3-compatible photo storage

- Implement =S3PhotoStore= with the existing opaque layout:
  =<prefix>/<storageKey>/<variant>.<extension>=. Upload the already processed
  files with the correct content type and immutable cache metadata.
- Keep uploads bounded and streaming. Since each generated variant originates
  from a maximum 10 MB input, prefer simple object uploads unless measurement
  shows multipart transfer is useful; if multipart is used, abort incomplete
  uploads on cancellation and configure provider cleanup.
- Make delete idempotent across all variants. Paginate object listings and
  preserve the one-hour orphan grace period used by reconciliation.
- Define readiness as a cheap bucket-access check and perform a write/read/delete
  probe at startup or deployment verification. Distinguish missing credentials,
  missing bucket, permission failures, timeouts, and unavailable storage in
  logs without exposing secrets.
- Grant the application credentials access only to the chosen bucket and prefix
  for the minimum required get, put, delete, list, and bucket-check operations.
  Do not use MinIO root credentials or cloud account-owner credentials.
- Add a pinned S3-compatible service and bucket initializer to the development
  Compose profile. Keep the existing filesystem profile so normal laptop
  startup does not require another stateful service.
- Run a shared =PhotoStore= contract suite against the filesystem and
  S3-compatible interpreters. Cover all variants, metadata, overwrite
  semantics, paginated listing, aged-orphan cleanup, partial-write
  compensation, unavailable storage, and deletion.
- Add opt-in provider smoke tests for AWS S3 and the selected cloud provider.
  Verify custom endpoint, signing region, addressing style, listing, and delete
  behavior rather than assuming complete S3 parity.

Current progress: the AWS SDK v2 asynchronous client is pinned and backs an
=S3PhotoStore= with metadata-rich immutable writes, idempotent prefix deletion,
paginated listing, aged-orphan support, bounded concurrent operations, and a
cheap bucket-access readiness check. An opt-in pinned MinIO Compose profile
creates a development bucket and non-root application credential. The shared
contract suite passes locally and against that endpoint, including custom
endpoint, signing-region, path-style, upload/read/overwrite/list/delete
behavior. The unchanged full HTTP/browser photo flows and provider smoke tests
remain to be run before this phase can meet its exit criterion.

Exit criterion: the unchanged photo API and browser flows pass against both
backends, and an uploaded photo remains readable after an application restart
with no local photo volume attached.

** TODO Phase 12: Photo migration, backups, and recovery

- Build an idempotent migration command that inventories database storage keys,
  copies every local original/display/thumbnail object, verifies size and a
  content checksum, reports missing or orphaned data, and can resume safely.
  Provide dry-run and machine-readable summary modes.
- Use a maintenance-window cutover for this small deployment: take a database
  backup, stop photo writes, run the final sync and verification, switch the
  storage backend, then exercise representative media before reopening writes.
- Keep the old local photo volume read-only for a documented rollback window.
  Rollback changes configuration only; do not delete source photos until the
  cloud deployment and restore rehearsal have passed.
- Use managed PostgreSQL backups with point-in-time recovery where the provider
  supports it. Define retention, ownership, and a periodic export independent
  of the application container.
- Define object recovery separately from live deletion. UI deletion remains
  immediate and no soft-delete model is added, but encrypted backups or bucket
  versions may retain deleted bytes for a fixed, documented retention window
  before permanent expiry.
- Keep database and object recovery consistent by recording the backup
  procedure, storage prefix, application version, and migration version
  together. Rehearse restoring both into an isolated environment and verify
  login, recipe search, and sampled original/display/thumbnail objects.
- Add storage-cost and backup-failure alerts. At the expected scale, favor a
  simple scheduled verification/export over a new in-application backup
  subsystem.

Exit criterion: all laptop photos are checksum-verified in object storage, a
documented rollback remains available, and an isolated database-plus-photo
restore has succeeded.

** TODO Phase 13: Cloud deployment hardening

- Add a cloud deployment profile for one application replica, a managed
  PostgreSQL database, and a private S3-compatible bucket. Start with a small
  database pool and JVM memory limit appropriate to the selected hosting tier.
- Terminate TLS at the hosting platform, force secure cookies, and verify the
  externally visible scheme/host behavior through the proxy. Trust forwarded
  headers only from the platform's proxy boundary.
- Retain default-deny application routes. Use a platform-compatible process
  health probe without exposing authenticated health details or metrics to the
  public internet.
- Add bounded login-failure protection and operational recovery instructions
  for rotating the configured admin password and invalidating existing
  sessions. Do not add user registration, password reset email, roles, or
  per-user ownership for this release.
- Store database, login, and object-storage credentials only in the hosting
  secret manager. Document rotation and ensure logs, deployment manifests,
  diagnostic endpoints, and exception messages do not reveal them.
- Decide how Flyway runs during deployment. With one replica it may run at
  application startup; every migration must remain compatible with the
  documented rollback window and a backup must precede destructive changes.
- Configure log retention, an uptime check, database/storage availability
  alerts, terminal scrape-job alerts only when scraping is enabled, and a
  conservative monthly spending alert.
- Update the production runbook for provisioning, deployment, migration,
  observation, secret rotation, backup/restore, rollback, and provider
  portability. Record the exact S3 features relied upon.

Exit criterion: the cloud stack is HTTPS-only, uses least-privilege secrets,
passes authenticated readiness checks, emits actionable alerts, and can be
redeployed without losing sessions, recipes, or photos.

** TODO Phase 14: Production release and cutover

- Run the clean Scala/PostgreSQL suite and browser workflows with scraping both
  enabled and disabled, then run the S3 contract suite and selected-provider
  smoke test.
- Test production limits and failure paths: wrong bucket credentials, storage
  timeout, database outage, interrupted upload, failed compensation, disabled
  scraping with queued work, login throttling, secure-cookie behavior, and
  application restart.
- Deploy a release candidate, import a copy of the laptop data, and soak it
  privately before changing the primary URL. Compare recipe, meal, photo, and
  reference counts plus representative search results.
- Perform the maintenance-window final sync from Phase 12, deploy one pinned
  application image, verify login/search/upload/read/delete/restart, and keep
  the prior laptop release and image available during the rollback window.
- After the rollback window, remove retained local photo copies according to
  the documented retention policy and record the release baseline.

Exit criterion: the primary cloud deployment has passed the full user workflow,
restart, migration, alert, backup, and restore checks, with a tested rollback
path and no dependency on an application-local photo volume.

** TODO Phase 15: Deliberate recipe sharing

- Define explicitly shareable recipe and meal views as revocable, opaque
  capabilities rather than making ordinary application routes public.
- Decide sharing scope, expiry, regeneration/revocation behavior, whether
  original photo downloads are allowed, and what metadata/imported content is
  appropriate for an unauthenticated visitor.
- Keep every non-shared route behind the existing default-deny authentication
  boundary. Share tokens must be unguessable, stored only as hashes, and have
  rate/abuse limits suitable for the public deployment.
- Provide a clear UI for creating, copying, reviewing, and revoking a share
  link, plus route and authorization coverage.

Exit criterion: a user can intentionally share a scoped recipe or meal link,
revoke it immediately, and no unrelated application resource becomes public.

** TODO Phase 16: Book-source image OCR and assisted recipe transcription

- Let a user optionally attach one or more book-page images when creating or
  editing a book reference, with the same authenticated upload, size, media,
  and permanent-deletion guarantees as meal photos.
- Store book-source image metadata through a dedicated relationship rather than
  treating it as a meal photo. Keep private images behind the existing
  authentication boundary.
- Process transcription asynchronously and durably. Use OCR to extract text,
  then evaluate an optional LLM-assisted step for turning the transcription
  into a reviewable recipe/ingredient/instruction draft; never silently replace
  user-authored recipe content.
- Define provider, cost, retention, prompt-data privacy, failure/retry, and
  human-review policies before enabling external AI processing. Preserve the
  raw OCR text and show processing state plus errors in the book-source UI.
- Add unit, integration, and browser coverage for image upload, authorization,
  OCR job durability, failure/retry, draft review, and deletion cleanup.

Exit criterion: an authenticated user can attach book-page photos to a book
source, receive a durable reviewed transcription draft, and safely keep, edit,
or discard it without exposing the images or changing the original recipe.

* Definition of Done for the MVP

- =GET /login= and =POST /login= are the only public routes; =admin= / =test=
  creates a session and every other route rejects unauthenticated access.
- Sessions last exactly 24 hours and remain valid across application restarts.
- All authenticated users access the same shared recipe collection.
- A user can create a recipe, add references, and continue without waiting for
  scraping.
- Scrape work is durable, bounded, observable, retryable, and safe against local
  network access.
- A recipe can contain multiple cooking events, each with its own date, notes,
  and photos.
- JPEG, PNG, and WebP photos up to 10 MB can be uploaded; other formats and
  oversized files are rejected safely.
- The most recent cooking date and chosen/fallback primary photo appear on the
  home card.
- Incremental search finds recipes using titles, descriptions, citations,
  imported text, and meal notes.
- CRUD endpoints, main pages, migrations, and background jobs have automated
  coverage for normal and important failure paths.
- Data constraints prevent orphaned or cross-recipe records.
- A fresh environment can be started from documentation and the versioned
  application image can run on this laptop with persistent database and photo
  volumes.

* Recommended First Vertical Slice

After Phase 1, build one thin end-to-end workflow before filling out every CRUD
operation:

1. Create a recipe with one URL.
2. Enqueue and execute its scrape job.
3. Build its search document.
4. Find it from the home search box.
5. Open its detail page.

This validates the highest-risk integration points early: HTTP routes, doobie
transactions, worker lifecycle, extraction, indexing, and browser interaction.
Then add meals and photos on top of the proven path.

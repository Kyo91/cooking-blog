#!/usr/bin/env sh
# Starts the laptop release in the background and waits for every health check.
set -eu

script_directory=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
exec "$script_directory/release-compose" up -d --wait

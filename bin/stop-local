#!/usr/bin/env sh
# Stops and removes release containers and networking without deleting persistent volumes.
set -eu

script_directory=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
exec "$script_directory/release-compose" down

#!/usr/bin/env sh
# Restarts only the application container while preserving its database and photo volumes.
set -eu

script_directory=$(CDPATH= cd -- "$(dirname "$0")" && pwd)
exec "$script_directory/release-compose" restart app
